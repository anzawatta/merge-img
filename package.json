{
  "name": "merge-img",
  "version": "2.1.2",
  "private": true,
  "description": "Merge multiple images into a single image",
  "author": "Plusb Preco <plusb21@gmail.com>",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "npm run lint && npm run test && npm run build",
    "prebuild": "npm run clean",
    "test": "jest test",
    "lint": "eslint .",
    "build": "babel src --out-dir lib",
    "clean": "node -e 'fs.rmSync(`lib`, { recursive: true })'"
  },
  "engines": {
    "node": ">= 16"
  },
  "license": "MIT",
  "repository": "anzawatta/merge-img",
  "files": [
    "lib"
  ],
  "keywords": [
    "concat",
    "convert",
    "compose",
    "image",
    "manipulation",
    "merge"
  ],
  "dependencies": {
    "is-plain-obj": "^4.0.0",
    "jimp": "^0.16.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.17.6",
    "@babel/core": "^7.17.9",
    "@babel/preset-env": "^7.16.11",
    "babel-jest": "^28.0.2",
    "eslint": "^8.14.0",
    "jest": "^28.0.2"
  }
}
